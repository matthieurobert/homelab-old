apiVersion: v1
kind: Namespace
metadata:
  name: docker
---
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: docker
spec:
  type: ExternalName
  externalName: grafana.homelab.mrobert.fr
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: docker
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    ingress.kubernetes.io/secure-backends: "true"
    ingress.kubernetes.io/preserve-host: "false"
    nginx.ingress.kubernetes.io/backend-protocol: HTTPS
    nginx.ingress.kubernetes.io/upstream-vhost: grafana.homelab.mrobert.fr
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - grafana.kube.homelab.mrobert.fr
    secretName: grafana.kube.homelab.mrobert.fr-tls
  rules:
  - host: grafana.kube.homelab.mrobert.fr
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: grafana
            port:
              number: 443
